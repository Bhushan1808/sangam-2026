# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          # Provide a Firebase service account JSON as a repository secret.
          # Create a secret named `FIREBASE_SERVICE_ACCOUNT` in your repo settings
          # and paste the full JSON key for a service account with Hosting permissions.
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_wnh_sangam2026 }}
          channelId: live
          projectId: wnh-sangam2026
